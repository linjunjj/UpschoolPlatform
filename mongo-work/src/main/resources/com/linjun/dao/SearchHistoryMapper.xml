<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linjun.dao.SearchHistoryMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.linjun.entity.SearchHistoryEntity" id="searchHistoryMap">
        <result property="id" column="id"/>
        <result property="keyword" column="keyword"/>
        <result property="from" column="from"/>
        <result property="add_time" column="add_time"/>
        <result property="user_id" column="user_id"/>
    </resultMap>

    <select id="queryObject" resultMap="searchHistoryMap">
        select * from search_history where msg_id = #{value}
    </select>

    <select id="queryList" resultMap="searchHistoryMap">
        select * from search_history
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by msg_id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
        select count(*) from search_history
    </select>

    <insert id="save" parameterType="com.linjun.entity.SearchHistoryEntity" useGeneratedKeys="true" keyProperty="msgId">
        insert into search_history(
        `keyword`,
        `from`,
        `add_time`,
        `user_id`
        values(
        #{keyword},
        #{from},
        #{add_time},
        #{user_id}
        )
    </insert>

    <update id="update" parameterType="com.linjun.entity.SearchHistoryEntity">
        update search_history
        <set>
            <if test="keyword != null">`keyword` = #{keyword}, </if>
            <if test="from != null">`from` = #{from}, </if>
            <if test="add_time != null">`add_time` = #{add_time}, </if>
            <if test="user_id != null">`user_id` = #{user_id}, </if>
        </set>
        where id = #{Id}
    </update>

    <delete id="delete">
        delete from search_history where id = #{value}
    </delete>

    <delete id="deleteBatch">
        delete from search_history where id in
        <foreach item="msgId" collection="array" open="(" separator="," close=")">
            #{Id}
        </foreach>
    </delete>

</mapper>